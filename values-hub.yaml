# Schedules are source-cluster only (hub creates snapshots for replication)
tridentProtect:
  snapshots:
    - name: init-boutique
      applicationRef: boutique
      appVaultRef: s3-appvault
      reclaimPolicy: Delete
    - name: init-wordpress
      applicationRef: wordpress
      appVaultRef: s3-appvault
      reclaimPolicy: Delete
  schedules:
    - name: boutique-hourly
      applicationRef: boutique
      appVaultRef: s3-appvault
      granularity: Hourly
      minute: "0"
      snapshotRetention: "24"
      backupRetention: "7"
    - name: wordpress-hourly
      applicationRef: wordpress
      appVaultRef: s3-appvault
      granularity: Hourly
      minute: "0"
      snapshotRetention: "24"
      backupRetention: "7"
# FSx endpoint IPs are dynamically discovered by fsx-endpoint-watcher CronJob.
# Only SVM names are needed here (deterministic, set by crossplane-setup).
tridentFSX:
  mgmtLIF: ""
  clusterLIF: ""
  svm: SVM1
  interclusterIPs: []
  peer:
    mgmtLIF: ""
    clusterLIF: ""
    svm: SVM2
    interclusterIPs: []
clusterGroup:
  name: hub
  argoCD:
    resourceHealthChecks:
      - kind: TridentOrchestrator
        group: trident.netapp.io
        check: |
          hs = {}
          if obj.status ~= nil and obj.status.status ~= nil then
            if obj.status.status == "Installed" then
              hs.status = "Healthy"
              hs.message = "Trident installation completed successfully."
            elseif obj.status.status == "Installing" then
              hs.status = "Progressing"
              hs.message = "Trident is currently installing."
            elseif obj.status.status == "Updating" then
              hs.status = "Progressing"
              hs.message = "Trident is currently updating."
            elseif obj.status.status == "Failed" then
              hs.status = "Degraded"
              hs.message = "Trident installation failed. Check operator logs."
            else
              hs.status = "Progressing"
              hs.message = "Trident status unknown or pending."
            end
          else
            hs.status = "Progressing"
            hs.message = "Awaiting TridentOrchestrator status."
          end
          return hs
      # Crossplane FSx: Ready=False during AWS CREATING should be Progressing, not Degraded.
      - kind: OntapFileSystem
        group: fsx.aws.upbound.io
        check: |
          hs = {}
          local ready = nil
          local synced = nil
          local sync_msg = ""
          local ready_msg = ""
          local ready_reason = ""
          if obj.status ~= nil and obj.status.conditions ~= nil then
            for i, c in ipairs(obj.status.conditions) do
              if c.type == "Ready" then
                ready = c.status
                ready_reason = c.reason or ""
                ready_msg = c.message or ""
              elseif c.type == "Synced" then
                synced = c.status
                sync_msg = c.message or ""
              end
            end
          end
          if synced == "False" then
            hs.status = "Degraded"
            hs.message = "Provider sync failed"
            if sync_msg ~= "" then
              hs.message = hs.message .. ": " .. sync_msg
            end
            return hs
          end
          if ready == "True" then
            hs.status = "Healthy"
            hs.message = "FSx ONTAP filesystem is ready."
            return hs
          end
          if ready == "False" then
            local r = string.lower(ready_reason or "")
            if string.find(r, "error") ~= nil or string.find(r, "fail") ~= nil then
              hs.status = "Degraded"
              hs.message = ready_msg ~= "" and ready_msg or ready_reason
              return hs
            end
            hs.status = "Progressing"
            hs.message = "FSx filesystem provisioning: " .. (ready_reason or "in progress")
            if ready_msg ~= "" then
              hs.message = hs.message .. " - " .. ready_msg
            end
            return hs
          end
          hs.status = "Progressing"
          hs.message = "Awaiting FSx OntapFileSystem status."
          return hs
      - kind: OntapStorageVirtualMachine
        group: fsx.aws.upbound.io
        check: |
          hs = {}
          local ready = nil
          local synced = nil
          local sync_msg = ""
          local ready_msg = ""
          local ready_reason = ""
          if obj.status ~= nil and obj.status.conditions ~= nil then
            for i, c in ipairs(obj.status.conditions) do
              if c.type == "Ready" then
                ready = c.status
                ready_reason = c.reason or ""
                ready_msg = c.message or ""
              elseif c.type == "Synced" then
                synced = c.status
                sync_msg = c.message or ""
              end
            end
          end
          if synced == "False" then
            hs.status = "Degraded"
            hs.message = "Provider sync failed"
            if sync_msg ~= "" then
              hs.message = hs.message .. ": " .. sync_msg
            end
            return hs
          end
          if ready == "True" then
            hs.status = "Healthy"
            hs.message = "FSx SVM is ready."
            return hs
          end
          if ready == "False" then
            local r = string.lower(ready_reason or "")
            if string.find(r, "error") ~= nil or string.find(r, "fail") ~= nil then
              hs.status = "Degraded"
              hs.message = ready_msg ~= "" and ready_msg or ready_reason
              return hs
            end
            hs.status = "Progressing"
            hs.message = "FSx SVM provisioning: " .. (ready_reason or "in progress")
            if ready_msg ~= "" then
              hs.message = hs.message .. " - " .. ready_msg
            end
            return hs
          end
          hs.status = "Progressing"
          hs.message = "Awaiting FSx OntapStorageVirtualMachine status."
          return hs
    resourceActions:
      - group: protect.trident.netapp.io
        kind: AppMirrorRelationship
        action: |
          discovery.lua: |
            actions = {}
            actions["Failover"] = {["disabled"] = true}
            actions["Resync"] = {["disabled"] = true}
            if obj.spec ~= nil and obj.spec.desiredState ~= nil then
              if obj.spec.desiredState == "Established" then
                actions["Failover"]["disabled"] = false
              elseif obj.spec.desiredState == "Promoted" then
                actions["Resync"]["disabled"] = false
              end
            end
            return actions
          definitions:
          - name: Failover
            action.lua: |
              obj.spec.desiredState = "Promoted"
              return obj
          - name: Resync
            action.lua: |
              obj.spec.desiredState = "Established"
              return obj
  # Namespace is a dictionary for easier overriding (lists are supported as well)
  namespaces:
    open-cluster-management:
    vault:
    golang-external-secrets:
    crossplane-system:
    trident:
    trident-protect:
    openshift-cnv:
    wordpress:
    boutique:
  subscriptions:
    acm:
      name: advanced-cluster-management
      namespace: open-cluster-management
      channel: release-2.15
      # csv: advanced-cluster-management.v2.6.1
    trident:
      name: trident-operator
      namespace: openshift-operators
      channel: stable
      source: certified-operators
  argoProjects:
    - hub
    - crossplane
    - trident
    - workload
  # Explicitly mention the cluster-state based overrides we plan to use for this pattern.
  # We can use self-referential variables because the chart calls the tpl function with these variables defined
  sharedValueFiles:
    - '/overrides/values-{{ $.Values.global.clusterPlatform }}.yaml'
    - '/overrides/values-{{ $.Values.global.clusterVersion }}-{{ $.Values.clusterGroup.name }}.yaml'
  # sharedValueFiles is a flexible mechanism that will add the listed valuefiles to every app defined in the
  # applications section. We intend this to supplement and possibly even replace previous "magic" mechanisms, though
  # we do not at present have a target date for removal.
  #
  # To replicate the "classic" magic include structure, the clusterGroup would need all of these
  # sharedValueFiles, in this order:
  #   - '/overrides/values-{{ $.Values.global.clusterPlatform }}.yaml'
  #   - '/overrides/values-{{ $.Values.global.clusterPlatform }}-{{ $.Values.global.clusterVersion }}.yaml'
  #   - '/overrides/values-{{ $.Values.global.clusterPlatform }}-{{ $.Values.clusterGroup.name }}.yaml'
  #   - '/overrides/values-{{ $.Values.global.clusterVersion }}-{{ $.Values.clusterGroup.name }}.yaml"
  #   - '/overrides/values-{{ $.Values.global.localClusterName }}.yaml'

  # This kind of variable substitution will work with any of the variables the Validated Patterns operator knows
  # about and sets, so this is also possible, for example:
  #   - '/overrides/values-{{ $.Values.global.hubClusterDomain }}.yaml'
  #   - '/overrides/values-{{ $.Values.global.localClusterDomain }}.yaml'
  applications:
    acm:
      name: acm
      namespace: open-cluster-management
      argoProject: hub
      chart: acm
      chartVersion: 0.1.*
    vault:
      name: vault
      namespace: vault
      argoProject: hub
      chart: hashicorp-vault
      chartVersion: 0.1.*
    golang-external-secrets:
      name: golang-external-secrets
      namespace: golang-external-secrets
      argoProject: hub
      chart: golang-external-secrets
      chartVersion: 0.1.*
    crossplane:
      name: crossplane
      namespace: crossplane-system
      argoProject: crossplane
      path: charts/all/crossplane
    crossplane-providers:
      name: crossplane-providers
      namespace: crossplane-system
      argoProject: crossplane
      path: charts/all/crossplane-providers
    crossplane-aws-infra:
      name: crossplane-aws-infra
      namespace: crossplane-system
      argoProject: crossplane
      path: charts/all/crossplane-aws-infra
      ignoreDifferences:
        - group: route53.aws.upbound.io
          kind: Record
          jqPathExpressions:
            - .spec.forProvider.healthCheckId
        - group: route53.aws.upbound.io
          kind: HealthCheck
          jqPathExpressions:
            - .spec.forProvider.enableSni
            - .spec.forProvider.tags
        - group: ec2.aws.upbound.io
          kind: VPCPeeringConnectionOptions
          jqPathExpressions:
            - .spec.managementPolicies
    trident:
      name: trident
      namespace: trident
      path: charts/all/trident
      argoProject: trident
      ignoreDifferences:
        - group: trident.netapp.io
          kind: TridentBackendConfig
          jqPathExpressions:
            - .spec.managementLIF
            - .spec.svm
            - .spec.replication
    protect:
      name: trident-protect
      namespace: trident-protect
      argoProject: trident
      chart: trident-protect
      repoURL: https://netapp.github.io/trident-protect-helm-chart
      chartVersion: 100.2510.0
      ignoreDifferences:
        - kind: Secret
          name: tp-webhook-tls
          jqPathExpressions:
            - .data
        - group: admissionregistration.k8s.io
          kind: ValidatingWebhookConfiguration
          name: trident-protect-validating-webhook-configurations
          jqPathExpressions:
            - '.webhooks[].clientConfig.caBundle'
    protect-config:
      name: trident-protect-config
      namespace: trident-protect
      argoProject: trident
      path: charts/all/trident-protect-config
      ignoreDifferences:
        - group: protect.trident.netapp.io
          kind: AppMirrorRelationship
          jqPathExpressions:
            - .spec.sourceApplicationUID
    wordpress:
      name: wordpress
      namespace: wordpress
      argoProject: workload
      path: charts/all/wordpress
    dr-monitoring:
      name: dr-monitoring
      namespace: openshift-monitoring
      argoProject: trident
      path: charts/all/dr-monitoring
      overrides:
        - name: drMonitoring.clusterRole
          value: hub
    boutique:
      name: boutique
      namespace: boutique
      chart: boutique
      chartVersion: 0.0.*
      argoProject: workload
      overrides:
        - name: global.storageClass
          value: storage-class-nfs
  imperative:
    # NOTE: We *must* use lists and not hashes. As hashes lose ordering once parsed by helm
    # The default schedule is every 10 minutes: imperative.schedule
    # Total timeout of all jobs is 1h: imperative.activeDeadlineSeconds
    # imagePullPolicy is set to always: imperative.imagePullPolicy
    # For additional overrides that apply to the jobs, please refer to
    # https://validatedpatterns.io/patterns/multicloud-gitops/mcg-imperative-actions/
    jobs:
      - name: hello-world
        # ansible playbook to be run
        playbook: rhvp.cluster_utils.hello_world
        # per playbook timeout in seconds
        timeout: 234
        # verbosity: "-v"
  managedClusterGroups:
    exampleRegion:
      name: secondary
      acmlabels:
        - name: clusterGroup
          value: secondary
          # helmOverrides:
          #   - name: foo
          #     value: bar
          # Before enabling cluster provisioning, ensure AWS and/or Azure
          #   credentials and OCP pull secrets are defined in Vault.
          #   See values-secret.yaml.template
          #
          # clusterPools:
          #  exampleAWSPool:
          #    name: aws-ap
          #    openshiftVersion: 4.10.18
          #    baseDomain: blueprints.rhecoeng.com
          #    platform:
          #      aws:
          #        region: ap-southeast-2
          #    clusters:
          #    - One
          #
          #  exampleAzurePool:
          #    name: azure-us
          #    openshiftVersion: 4.10.18
          #    baseDomain: blueprints.rhecoeng.com
          #    platform:
          #      azure:
          #        baseDomainResourceGroupName: dojo-dns-zones
          #        region: eastus
          #    clusters:
          #    - Two
          #    - Three
#  To have apps in multiple flavors, use namespaces and use helm overrides as appropriate
#
#    pipelines:
#      name: pipelines
#      namespace: production
#      project: datacenter
#      path: applications/pipeline
#      repoURL: https://github.com/you/applications.git
#      targetRevision: stable
#      overrides:
#      - name: myparam
#        value: myparam
#
#    pipelines_staging:
#    - name: pipelines
#      namespace: staging
#      project: datacenter
#      path: applications/pipeline
#      repoURL: https://github.com/you/applications.git
#      targetRevision: main
#
#   Additional applications
#   Be sure to include additional resources your apps will require
#   +X machines
#   +Y RAM
#   +Z CPU
#    vendor-app:
#      name: vendor-app
#      namespace: default
#      project: vendor
#      path: path/to/myapp
#      repoURL: https://github.com/vendor/applications.git
#      targetRevision: main
#  managedSites:
#    factory:
#      name: factory
#      # repoURL: https://github.com/dagger-refuse-cool/manuela-factory.git
#      targetRevision: main
#      path: applications/factory
#      helmOverrides:
#      - name: foo
#        value: bar
#      clusterSelector:
#        matchExpressions:
#        - key: vendor
#          operator: In
#          values:
#            - OpenShift

fsxOntap:
  enabled: true
  region: us-west-1
  fileSystemName: primary-us-west-1-fsx
  vpcId: vpc-0b20378d978812b38
  subnetIds:
    - subnet-0ee36e869d5d7888f
    - subnet-0b8b471177e57c605
  routeTableIds:
    - rtb-0f81ad3483d863b7b
    - rtb-027c5d854d53eb63a
  preferredSubnetId: subnet-0ee36e869d5d7888f
  allowedCidrs:
    - 10.0.0.0/16
    - 10.100.0.0/16
  storageCapacity: 1024
  throughputCapacity: 1024
  storageType: SSD
  deploymentType: MULTI_AZ_1
  svmName: SVM1
  peer:
    enabled: true
    region: us-west-2
    fileSystemName: secondary-us-west-2-fsx
    vpcId: vpc-04367ed69a97931c3
    subnetIds:
      - subnet-0e5eb4ffe543fbe5b
      - subnet-0b992181deb88f0b9
    routeTableIds:
      - rtb-08cf18f5db54c8413
      - rtb-0d279a771a3839dfa
      - rtb-04b3ddc21e099b0b8
      - rtb-078dc892b49a4dd79
    preferredSubnetId: subnet-0e5eb4ffe543fbe5b
    allowedCidrs:
      - 10.100.0.0/16
      - 10.0.0.0/16
    svmName: SVM2
s3AppVault:
  enabled: true
  bucketName: trident-protect-bucket
  region: us-west-1
endpointWatcher:
  enabled: true
  local:
    fileSystemName: primary-us-west-1-fsx
    region: us-west-1
    svmName: SVM1
  peer:
    fileSystemName: secondary-us-west-2-fsx
    region: us-west-2
    svmName: SVM2
vpcPeering:
  enabled: true
  prod:
    region: us-west-1
    vpcId: vpc-0b20378d978812b38
    vpcCidr: 10.0.0.0/16
    clusterName: primary
    routeTableIds:
      - rtb-0f3227cbadc03092d
      - rtb-0f81ad3483d863b7b
      - rtb-0c403bbb53cc2c137
      - rtb-027c5d854d53eb63a
  dr:
    region: us-west-2
    vpcId: vpc-04367ed69a97931c3
    vpcCidr: 10.100.0.0/16
    clusterName: secondary
    routeTableIds:
      - rtb-04b3ded48337bf7fb
      - rtb-0593653e79731bf8c
      - rtb-08cf18f5db54c8413
      - rtb-0d279a771a3839dfa
      - rtb-04b3ddc21e099b0b8
      - rtb-078dc892b49a4dd79
route53Failover:
  enabled: true
  domain: dr.aws.validatedpatterns.io
  # If false: set hostedZoneId to the Route53 zone ID for `domain` (see ansible dr_failover_hosted_zone_id or AWS console).
  # If true: Crossplane creates the zone; delegate NS at the parent registrar.
  createHostedZone: False
  hostedZoneId: "Z001281810MV5UN93UQKQ"
  primaryIngressHostname: router-default.apps.primary.aws.validatedpatterns.io
  secondaryIngressHostname: router-default.apps.secondary.aws.validatedpatterns.io
  healthCheckPath: /
  # Per-app primaryHealthCheckFqdn / secondaryHealthCheckFqdn are optional; these apply to all apps when omitted.
  primaryHealthCheckFqdn: router-default.apps.primary.aws.validatedpatterns.io
  secondaryHealthCheckFqdn: router-default.apps.secondary.aws.validatedpatterns.io
  apps:
    - name: boutique
    - name: wordpress
  region: us-west-1
